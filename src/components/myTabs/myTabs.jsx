
const myTabs = ({ tabData }) => {
    const {
      jobTitle,
      date,
      category,
      minimumPrice,
      maximumPrice,
      description,
      email,
    } = tabData
    console.log(tabData);
    return <h2>{ }jobTitle</h2>
}

export default myTabs
